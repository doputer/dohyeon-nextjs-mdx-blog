const Divider = () => (
  <div aria-hidden className="mx-auto flex gap-3 leading-none text-muted select-none">
    <span>*</span>
    <span>*</span>
    <span>*</span>
  </div>
);

export default Divider;
